<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.repository.ReplyDAO">

    <insert id="insert">
        INSERT INTO reply (bno, reply, replyer)
        VALUES (#{bno}, #{reply}, #{replyer})
    </insert>

    <select id="getList" resultType="com.example.demo.domain.ReplyVO">
        SELECT * FROM reply 
        WHERE bno = #{bno} 
        ORDER BY rno DESC 
        LIMIT #{pageStart}, #{qty}
    </select>

    <select id="getTotalCount" resultType="int">
        SELECT COUNT(rno) FROM reply WHERE bno = #{bno}
    </select>

    <update id="update">
        UPDATE reply SET reply = #{reply}, updateDate = now() 
        WHERE rno = #{rno}
    </update>

    <delete id="delete">
        DELETE FROM reply WHERE rno = #{rno}
    </delete>

</mapper>
